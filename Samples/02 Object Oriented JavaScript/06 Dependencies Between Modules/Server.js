var Server = {
};

(function () {
    Server.saveContact = function (contact) {
        //
        //  Do AJAX
        //

        console.log("Server: Saving a contact width id: " + contact.id);
    }
})();
